import styles from './CityFilter.module.css'

const CityFilter = () => (
  <div className={styles.CityFilter}>
    <label className={styles.Label}>South County</label>
  </div>
)

export default CityFilter
